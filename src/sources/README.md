# Source Adapters

Source-specific adapters will live here. Each adapter should hide the quirks of one upstream data source behind small, typed contracts before the MCP layer exposes tools or resources.

Shared contracts, errors, pagination helpers, and provenance types belong in `common/`.
