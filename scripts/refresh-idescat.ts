#!/usr/bin/env tsx
console.log(
  [
    "IDESCAT search index refresh is intentionally manual.",
    "The committed seed index is in src/sources/idescat/search-index/.",
    "Extend this script to crawl Tables v2 before replacing the generated modules.",
  ].join("\n"),
);
